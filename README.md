To compile the project you need to use commands:
cmake -B build
cmake --build build --config Release

On linux before compilation you need to download these libs:
libgl-dev
libvlc-dev
vlc
vlc-plugin-base
vlc-plugin-video-output


